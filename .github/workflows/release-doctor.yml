name: Release Doctor
on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  release_doctor:
    name: release doctor
    runs-on: ubuntu-latest
    if: github.repository == 'johnglavan71/MCP-Productions' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch' || startsWith(github.head_ref, 'release-please') || github.head_ref == 'next')

    steps:
      - uses: actions/checkout@v6

      - name: Check release environment
        run: |
          bash ./bin/check-release-environment
        env:
          NPM_TOKEN: ${{ secrets.TEST2W_NPM_TOKEN || secrets.NPM_TOKEN }}
          DOCKERHUB_TOKEN: ${{ secrets.TEST2W_DOCKERHUB_TOKEN || secrets.DOCKERHUB_TOKEN }}

